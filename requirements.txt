# Versões FIXAS — não alterar no painel Streamlit; só aqui no GitHub.
# Python 3.12 no Cloud (Settings → Advanced) + este arquivo.
streamlit==1.59.1
starlette==0.41.3
pandas>=2.0.0,<3.0.0
supabase>=2.0.0,<3.0.0
